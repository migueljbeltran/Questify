# Enforce LF line endings for all text files
* text=auto eol=lf

# Ensure shell scripts use LF (important for husky hooks)
*.sh text eol=lf
.husky/* text eol=lf

# Windows batch files use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
